for (let planaArray = 1; planaArray <= 1000; planaArray+=1) {
if (planaArray == 1000) {
        
    }

    setTimeout(() => {
        console.log('Un array es una manera de poder guardar datos del mismo tipo o clase ' + planaArray);
    }, 5000);
    
} 


